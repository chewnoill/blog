{
  "name": "blog-git-history",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "next": "^9.1.6",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "scripts": {
    "dev": "next",
    "build": "next build && next export",
    "start": "next start",
    "format-base": "prettier '**/*.{js,jsx,json}'",
    "format": "yarn format-base --write",
    "format-check": "yarn format-base -c"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^4.0.0",
    "prettier": "^1.19.1"
  }
}
